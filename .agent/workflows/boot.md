---
description: Read all mission-critical project documentation and align with guidelines.
---

Perform the following steps immediately to sync with the project status and rules:

1. **Read Tier 1 Documentation (Rules & Context)**:
   - Use `view_file` on `docs/AGENTS.md`
   - Use `view_file` on `docs/GUIDELINES.md`
   - Use `view_file` on `docs/PROJECT_CONTEXT.md`

2. **Read Tier 2 Documentation (Architecture & Tasks)**:
   - Use `view_file` on `docs/ARCHITECTURE.md`
   - Use `view_file` on `docs/TODO.md`

3. **Verification**:
   - Confirm to the user that you have read These files and are aligned with the Strict TypeScript and CSS Module requirements.
